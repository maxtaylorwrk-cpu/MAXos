# .env.example

# Example environment variables for local testing (do NOT commit secrets)
# BACKUP_DB_URL example: postgres://user:password@host:5432/postgres
BACKUP_DB_URL=
# Default backup output directory
BACKUP_DIR=$HOME/maxos-backups
# Optional: passphrase used for local encryption (recommend a password manager)
BACKUP_ENCRYPTION_PASSPHRASE=
